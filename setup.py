from setuptools import setup, find_packages

setup(
    name="mint",
    version="0.1.0",
    description="Lightweight utilities for DST on MultiWOZ",
    packages=find_packages(),
    include_package_data=True,
    python_requires=">=3.9",
    install_requires=[],
)
